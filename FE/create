server {
    listen 80;
    server_name betabase.pro www.betabase.pro;

    root /var/www/betabase/FE/build;   # use /dist if Vite
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
